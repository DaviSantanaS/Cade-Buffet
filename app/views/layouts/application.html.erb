<!DOCTYPE html>
<html lang="en">
  <head>
    <title>CadeBuffet</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application" %>
  </head>

  <body class="centered-content">
    <header>
      <nav>
        <ul>
          <li><%= link_to "Home", root_path %></li>
          <% if current_user %>
            <li class="user-greeting">Hello, <% if current_user.buffet_owner? %>Buffet owner
              <% else %>User
              <% end %>
              <%= current_user.name %></li>
            <li><%= button_to "Logout", destroy_user_session_path, method: :delete %></li>
          <% else %>
            <li><%= link_to "Login", new_user_session_path %></li>
          <% end %>
        </ul>
        <%= render 'buffets/search_form' %>
      </nav>
    </header>
    <main>
      <div>
        <%= alert%>
        <%= notice%>
      </div>
      <%= yield %>
  </main>
  </body>
</html>
